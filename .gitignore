# Logs
logs
logs_and_outputs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

node_modules
.env
.env.local
.env.*.local
.npmrc

# Private local material; never publish this folder.
ETC ETC DO NOT UPLOAD TO GITHUB/

# Database and storage backups (scripts/backup-safedrive.mjs). These contain
# KYC identity documents, selfies, addresses and payout account numbers -
# committing one would publish every user's ID. Keep the off-site copy in a
# PRIVATE cloud drive, never a shared link.
backups/

.vercel
.env*
